server:
  port: 8081


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservicedb
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver




  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect

  application:
    name: USER-SERVICE


  config:
    import: configserver:http://localhost:8085


#This is configuration can be used form configuration server which fetchs config form github above property
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka